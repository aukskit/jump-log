{
  "name": "jump-log",
	"displayName": "Jump Log",
	"description": "you can jump to the file from build log file.",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.62.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
        "onCommand:jump-log.helloWorld",
		"onCommand:jump-log.jumpToFile"
	],
	"main": "./src/extension.js",
	"contributes": {
		"commands": [
			{
				"command": "jump-log.helloWorld",
				"title": "Hello World"
			},
			{
				"command": "jump-log.jumpToFile",
				"title": "Jump To File"
			}
		],
		"configuration": {
			"title": "PATH",
			"properties": {
				"PATH.logfile": {
					"type": "string",
					"default": "",
					"description": "Path to .log file"
				},
				"PATH.projectOnUbuntu":{
					"type": "string",
					"default": "",
					"description": "Path to project on Ubuntu"
				},
				"PATH.projectOnWindows":{
					"type": "string",
					"default": "",
					"description": "Path to project on Windows(need to double escape...)"
				}
			}
		}
	},
	"scripts": {
		"lint": "eslint .",
		"pretest": "npm run lint",
		"test": "node ./test/runTest.js"
	},
	"devDependencies": {
        "@types/vscode": "^1.62.0",
        "@types/glob": "^7.1.4",
        "@types/mocha": "^9.0.0",
        "@types/node": "14.x",
        "eslint": "^8.1.0",
        "glob": "^7.1.7",
        "mocha": "^9.1.3",
        "typescript": "^4.4.4",
        "@vscode/test-electron": "^1.6.2"
    }

}
